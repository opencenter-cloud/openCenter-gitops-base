apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - "../sources/grafana.yaml"
  - helmrelease.yaml
secretGenerator:
  - name: loki-values-base
    namespace: observability
    type: Opaque
    files:
      - values.yaml=helm-values/values-v6.55.0.yaml
    options:
      disableNameSuffixHash: true
