---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - "../sources/grafana.yaml"
  - "helmrelease.yaml"
secretGenerator:
  - name: tempo-values-base
    namespace: observability
    type: Opaque
    files:
      - values.yaml=helm-values/hardened-values-1.61.3.yaml
    options:
      disableNameSuffixHash: true
