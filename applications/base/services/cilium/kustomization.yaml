apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - source.yaml
  - helmrelease.yaml
secretGenerator:
  - name: cilium-values-base
    namespace: kube-system
    type: Opaque
    files:
      - values.yaml=helm-values/values-v1.19.4.yaml
    options:
      disableNameSuffixHash: true
