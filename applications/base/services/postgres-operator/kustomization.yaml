apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
    - namespace.yaml
    - source.yaml
    - helmrelease.yaml
secretGenerator:
    - name: postgres-operator-values-base
      namespace: postgres-operator
      type: Opaque
      files:
        - values.yaml=helm-values/values-1.15.1.yaml
      options:
        disableNameSuffixHash: true
